                     <!-- joinModal -->
                     
                      <div id="joinModal" class="modal">
                        <div class="modal-background"></div>
                        <div class="modal-card">
                          <header class="modal-card-head">
                            <p class="modal-card-title">Join <strong><%=circle.title%></strong>?</p>
                            <button class="delete closeModal" aria-label="close"></button>
                          </header>
                          <section class="modal-card-body">
                            <!-- Content ... -->
                            
                                  <p style="font-size: .8em">circle price: $<%=circle.price%> <br>processing: $<%= (Number(circle.price)*.03).toFixed(2)%></p>
                                  <p><strong>Total: $<%= ((Number(circle.price)*.03) + Number(circle.price)).toFixed(2)%></strong></p>

                  
                          </section>
                          <footer class="modal-card-foot">
                            <form action="/agreements/new" method="POST">
                                    <div class="form-group">
                                        <input type="hidden" name="circle[id]" value="<%=circle.id%>">
                                        <button type="submit" class="button is-primary is-medium"><span class="icon"><i class="fa fa-plus-circle"></i></span> <span> join <%=circle.title%></span></button>
                                    </div>
                            </form>
                          </footer>
                        </div>
                      </div>
                     
                        
                      
                        
                        
      